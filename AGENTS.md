# 坦克大战 (Tank Battalion)

经典街机游戏《坦克大战》(Battle City) 的网页复刻版。玩家操控坦克保护底部的老鹰基地，消灭一波波敌方坦克。支持 PC 浏览器（横屏）和手机浏览器（竖屏），提供键盘、Xbox 手柄、触屏虚拟按键三种操作方式。

## 产品介绍

### 核心玩法
- 13×13 格子战场，5 个关卡循环
- 6 种地形：砖墙（可摧毁）、钢墙（满级可破）、水（坦克不可过）、树林（可穿过）、冰面、老鹰基地
- 4 种敌人：轻战车(100分)、装甲车(200分/最快)、速射炮(300分)、重型(400分/需4发)
- 6 种道具：加命♥、升级★、炸弹✸、冻结时钟◷、护盾⛑、基地加固⚒
- 胜负条件：老鹰被摧毁或生命耗尽 → 游戏结束；消灭全部敌人 → 通关

### 游戏规则
- 保护底部的老鹰基地，基地被摧毁则游戏结束
- 消灭所有敌人即可通关进入下一关
- 玩家有 3 条生命，全部失去则游戏结束
- 闪烁的红色敌人被击毁后会掉落道具

### 敌人类型
| 类型 | 分数 | 特点 |
|------|------|------|
| 轻战车 | 100 | 普通 |
| 装甲车 | 200 | 移动最快 |
| 速射炮战车 | 300 | 子弹速度快 |
| 重型战车 | 400 | 需 4 发炮弹击毁 |

## 工程介绍

### 技术栈
- **TypeScript** + **Vite** 构建
- **Canvas 2D** 渲染（所有素材通过代码绘制，无外部图片/音频依赖）
- **Vitest** 单元测试

### 目录结构
```
.
├── index.html              # 入口 HTML
├── package.json
├── tsconfig.json
├── vite.config.ts
├── src/
│   ├── main.ts             # 入口：画布、布局、游戏循环
│   └── game/
│       ├── types.ts        # 类型定义（TileType, TankKind, PowerUpType 等）
│       ├── constants.ts    # 常量（地图尺寸、速度、分数等）
│       ├── levels.ts       # 关卡地图定义与解析
│       ├── utils.ts        # 工具函数（碰撞检测、坐标转换等）
│       ├── Tank.ts         # 坦克基类（移动、碰撞、射击）
│       ├── PlayerTank.ts   # 玩家坦克（升级、护盾、生命）
│       ├── EnemyTank.ts    # 敌方坦克（AI、血量、道具掉落）
│       ├── Bullet.ts       # 子弹
│       ├── PowerUp.ts      # 道具
│       ├── InputManager.ts # 输入管理（键盘+手柄+触屏）
│       ├── TouchControls.ts# 触屏虚拟按键 UI
│       ├── Renderer.ts     # 渲染器（地形、坦克、子弹、UI）
│       └── Game.ts         # 游戏主类（状态、更新、碰撞、关卡）
├── tests/                  # 单元测试 + 冒烟/玩法测试
└── dist/                   # 构建产物
```

### 关键设计
- 坦克占 2×2 格（64×64px），移动时对齐半格网格以便转弯
- 子弹与地形碰撞：砖墙直接摧毁，钢墙需 3 星玩家（bulletPower=1），老鹰被击中即 Game Over
- 敌人 AI：定时换方向，70% 概率朝玩家/基地方向移动，射击由冷却+随机控制
- 道具：第 4/11/18 个出生的敌人携带道具，击毁后掉落

## 部署说明

### 开发模式
```bash
npm install
npm run dev          # http://localhost:5173
```

### 生产构建
```bash
npm run build        # 产物输出到 dist/
```

### 分发
`dist/` 目录为独立可运行的静态文件包：
```
dist/
├── index.html
└── assets/
    └── index-*.js
```
将 `dist/` 整体部署到任意静态文件服务器（Nginx、GitHub Pages、Vercel 等）即可。也可直接用浏览器打开 `dist/index.html` 本地运行（已配置相对路径 `base: './'`）。

所有游戏素材均通过 Canvas 代码绘制，无外部图片/音频资源依赖。

### 测试
```bash
npm test                       # 运行单元测试
npx tsx tests/smoke.ts         # 冒烟测试（600帧无错误）
npx tsx tests/gameplay.ts      # 玩法验证（射击/得分）
```

## 使用说明

### 操作
| 输入方式 | 移动 | 射击 | 暂停 |
|---------|------|------|------|
| 键盘 | W A S D / 方向键 | J / 空格 | P / Enter |
| Xbox 手柄 | 十字键 | A / X | Start |
| 触屏 | 左侧虚拟方向键 | 右侧红色射击键 | 右上角 II |

### 道具
| 道具 | 效果 |
|------|------|
| ♥ 战车 | 增加 1 条生命 |
| ★ 星星 | 升级坦克（子弹速度→双发→破钢墙） |
| ✸ 炸弹 | 消灭屏幕上所有敌人 |
| ◷ 时钟 | 冻结所有敌人 10 秒 |
| ⛑ 钢盔 | 获得 10 秒护盾 |
| ⚒ 铲子 | 基地砖墙变钢墙 21 秒 |

### 升级机制
玩家通过拾取 ★ 星星升级，最多 3 级：
1. 1 星：子弹速度提升
2. 2 星：可同时发射 2 发子弹
3. 3 星：可摧毁钢墙
